query FetchTaskDetails($id: ID!) {
  pipeline(id: $id) {
    id
    name
    description

    variables {
      key
      defaultValue
      exampleValue
      description

      constraints {
        selection
      }
    }
  }
}
