query FetchTaskDetails($id: ID!) {
  task(id: $id) {
    id
    name
    description
    labels

    variables {
      key
      defaultValue
      exampleValue
      description

      constraints {
        selection
      }

      valueAdvertisers {
        id
        name
        description
      }
    }
  }
}
