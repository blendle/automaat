query FetchTaskDetails($id: ID!) {
  task(id: $id) {
    id
    name
    description

    variables {
      key
      defaultValue
      exampleValue
      description

      constraints {
        selection
      }
    }
  }
}
