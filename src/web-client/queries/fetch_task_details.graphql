query FetchTaskDetails($id: ID!) {
  pipeline(id: $id) {
    id
    name
    description

    variables {
      key
      defaultValue
      description

      constraints {
        selection
      }
    }
  }
}
